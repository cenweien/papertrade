# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/
*.tsbuildinfo

# Generated by tsc -b for vite.config.ts (composite project)
frontend/vite.config.js
frontend/vite.config.d.ts

# Local env files (NEVER commit Supabase keys)
.env
.env.local
.env.*.local
frontend/.env.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor / IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
*.swp
*.swo
*~
*.bak
*.orig
*.rej
.DS_Store
Thumbs.db

# Test / coverage
coverage/
.nyc_output/

# Misc
.cache/
.parcel-cache/
.turbo/
.tmp/
.vercel
.env*
!.env.example

# ----------------------------------------------------------------------------
# Bloomberg SAPI integration
# ----------------------------------------------------------------------------
# Extracted wheel source - do not commit (install via pip from the .whl)
xbbg_sapi_extracted/
# The wheel itself is local-only; do not commit
xbbg_sapi-*.whl
# Any Python venv created to install xbbg_sapi or the relay service
.venv*/
.venv-bb/
bloomberg-service/.venv/
bloomberg-service/.env
bloomberg-service/**/__pycache__/
bloomberg-service/**/*.pyc

# ----------------------------------------------------------------------------
# Local tooling caches
# ----------------------------------------------------------------------------
# Chroma vector DB cache (used by indexing/ scripts)
.chroma/
# Supabase CLI temp / state files
supabase/.temp/
supabase/supabase/
# Empty root package files (this repo's frontend has its own package.json)
package.json
package-lock.json
pnpm-lock.yaml
bun.lock
yarn.lock
